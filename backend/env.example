## Backend env (copy to backend/.env for local dev)
##
## IMPORTANT:
## - Never commit backend/.env
## - If your DB password contains special characters (@ # ? / etc),
##   URL-encode it before using it in DATABASE_URL.
##
## Direct connection (recommended):
## DATABASE_URL=postgresql://postgres:<URL_ENCODED_PASSWORD>@db.<PROJECT_REF>.supabase.co:5432/postgres?sslmode=require

DATABASE_URL=
SECRET_KEY=
DEBUG=False

# Comma-separated list
CORS_ORIGINS=http://localhost:5173

# Database Pool Settings (for 1000+ concurrent users)
# Adjust based on your database tier and expected load
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=40
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800

# Rate Limiting (requests per window)
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# Demo Account Configuration
# Set DEMO_ENABLED=False in production if you want to disable demo access
DEMO_ENABLED=True
DEMO_CUSTOMER_EMAIL=demo.customer@maidease.com
DEMO_MAID_EMAIL=demo.maid@maidease.com
DEMO_PASSWORD=DemoPass123
